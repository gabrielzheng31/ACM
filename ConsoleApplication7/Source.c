/*#include <stdio.h>
#define SPACE ' '
int main(void)
{
	char ch;

	ch = getchar();
	while (ch != '\n')
	{
		if(ch == SPACE)
			putchar(ch);
		else
			putchar(ch + 1);
			ch = getchar();
	}
	putchar(ch);
	return 0;
}*/

#include <stdio.h>

int main(void)
{
	int M, N;
	while (scanf("%d %d", &M, &N)!=EOF && M!=-1 && N!= -1)
	{
		int J[1000], F
	}
}